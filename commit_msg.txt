fix(settings): 修复自动快照设置保存后被重置为默认值

- 根因：主表单 zod schema(createSystemConfigFormSchema)只声明了
  email/notification/site/auth/watermark，提交时 autoSnapshot 被 zod strip 掉，
  后端 deepMerge 保留旧值→前端 reset 又用 DEFAULT_CONFIG 回填，表现为"恢复最早设置"
- 在 settings 页面 onSubmit 用 methods.getValues() 取完整表单值，把
  autoSnapshot(及 maintenance)补回提交数据，与 maintenance 的处理方式一致
